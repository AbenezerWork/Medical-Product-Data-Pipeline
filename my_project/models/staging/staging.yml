version: 2

sources:
  - name: raw

    schema: raw # The schema you created in the Python script
    tables:
      - name: telegram_messages
        description: "Raw messages scraped from public Telegram channels, stored in JSONB format."


      - name: image_detections
        description: "Raw object detection results from YOLOv8."

